# Synthetic register doc. Add a paired trap case whose answer is NOT in context.
id: grounded-qa/register-lookup-01
role: r8
rubric: grounded-qa
max_tokens: 256
context: |
  UART_CR (Control Register), offset 0x0C, reset value 0x0000.
    bit 0  UARTEN  UART enable
    bit 8  TXE     transmit enable
    bit 9  RXE     receive enable
prompt: |
  What is the reset value of UART_CR, and which bit enables the transmitter?
  Cite the register.
expected: |
  Reset value 0x0000; bit 8 (TXE) enables the transmitter. Must cite UART_CR.
