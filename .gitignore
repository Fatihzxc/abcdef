# Claude Code local (per-machine) settings
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Model weights & datasets (large binaries stay out of git)
*.gguf
*.safetensors
*.bin
*.pt
*.pth
models/
datasets/

# Eval data classification (see phase-0-bakeoff.md Step 4)
evals/cases/private/      # sensitive company cases — local + backup only
evals/results/            # raw model outputs — summaries go to docs/bakeoff/
